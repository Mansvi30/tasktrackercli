# tasktrackercli
🚀 A Python-based Task Tracker CLI that helps users manage tasks efficiently with CRUD operations, status tracking (todo, in-progress, done), and JSON file storage—all using Python's standard library.
